% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crit_attribute.R
\name{crit_attribute}
\alias{crit_attribute}
\alias{crit_nominal}
\alias{crit_ordinal}
\alias{crit_numeric}
\title{Attribute-based criterion templates}
\usage{
crit_nominal(attribute, weight = 1, name = attribute)

crit_ordinal(
  attribute,
  levels,
  weight = 1,
  indifference = as_quantile(0.5),
  similarity = as_quantile(0.2),
  veto = NULL,
  name = attribute
)

crit_numeric(
  attribute,
  weight = 1,
  transform = identity,
  indifference = as_quantile(0.5),
  similarity = as_quantile(0.2),
  veto = NULL,
  name = attribute
)
}
\arguments{
\item{attribute}{Name of a case attribute carried by the \code{traces} object.}

\item{weight}{A single positive weight.}

\item{name}{Criterion label; defaults to the attribute name.}

\item{levels}{For \code{crit_ordinal()}, the attribute values from lowest to
highest rank.}

\item{indifference, similarity, veto}{Thresholds; a number or a \code{\link[=as_quantile]{as_quantile()}}
specification. Defaults suit the direction and are overridable.}

\item{transform}{For \code{crit_numeric()}, a function applied to the numeric
attribute before differencing (e.g. \code{log}); defaults to \code{\link[=identity]{identity()}}.}
}
\value{
A \code{\link[=criterion]{criterion()}} object.
}
\description{
Criterion constructors for case-level attributes, covering the common
measurement scales. Each builds a measure from a named attribute of the
\code{traces} object.
}
\details{
\itemize{
\item \code{crit_nominal()} scores a pair 1 when the attribute is equal and 0
otherwise (similarity direction; thresholds preset, no veto).
\item \code{crit_ordinal()} uses the absolute rank difference \verb{|rank_a - rank_b|}
over the ordered \code{levels} (dissimilarity direction); it covers Likert
scales.
\item \code{crit_numeric()} uses \verb{|x_a - x_b|} after an optional \code{transform}
(dissimilarity direction); it covers quantitative, interval and
percentage scales.
}
}
\examples{
crit_nominal("status", weight = 0.3)
crit_ordinal("triage", levels = c("green", "yellow", "red"), weight = 0.2)
crit_numeric("age", weight = 0.2, transform = identity)
}
\seealso{
\code{\link[=crit_activity_profile]{crit_activity_profile()}} and the other process-aware helpers.
}
