% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_clusters}
\alias{validate_clusters}
\title{Internal cluster validity indices}
\usage{
validate_clusters(clust)
}
\arguments{
\item{clust}{An \code{outrank_clust} object from \code{\link[=cluster_traces]{cluster_traces()}}.}
}
\value{
A named numeric vector with \code{connectivity} and \code{dunn}.
}
\description{
\code{validate_clusters()} reports the connectivity and Dunn index of a
clustering, computed on the dissimilarity \code{as.dist(1 - S)}.
}
\details{
Connectivity (to be minimised) and the Dunn index (to be maximised) are
obtained from the \pkg{clValid} package. If \pkg{clValid} is not installed,
the function returns \code{NA} values with an informative message.
}
\examples{
m <- matrix(0.1, 6, 6); m[1:3, 1:3] <- 0.9; m[4:6, 4:6] <- 0.9
diag(m) <- 1; dimnames(m) <- list(1:6, 1:6)
crit <- criterion(m, "similarity", indifference = 0.3, similarity = 0.95)
tr <- as_traces(data.frame(case = as.character(1:6), act = "x",
                           ts = as.POSIXct("2020-01-01")),
                "case", "act", "ts")
clust <- cluster_traces(outrank_similarity(tr, crit), k = 2, seed = 1)
validate_clusters(clust)
}
