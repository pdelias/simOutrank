% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crit_process.R
\name{crit_process}
\alias{crit_process}
\alias{crit_activity_profile}
\alias{crit_transitions}
\alias{crit_edit_distance}
\alias{crit_trace_length}
\alias{crit_distinct_activities}
\alias{crit_duration}
\title{Process-aware criteria}
\usage{
crit_activity_profile(
  weight = 1,
  indifference = q(0.5),
  similarity = q(0.8),
  veto = NULL,
  name = "activity_profile"
)

crit_transitions(
  weight = 1,
  indifference = q(0.5),
  similarity = q(0.8),
  veto = NULL,
  name = "transitions"
)

crit_edit_distance(
  weight = 1,
  method = "osa",
  indifference = q(0.5),
  similarity = q(0.2),
  veto = NULL,
  name = "edit_distance"
)

crit_trace_length(
  weight = 1,
  indifference = q(0.5),
  similarity = q(0.2),
  veto = NULL,
  name = "trace_length"
)

crit_distinct_activities(
  weight = 1,
  indifference = q(0.5),
  similarity = q(0.2),
  veto = NULL,
  name = "distinct_activities"
)

crit_duration(
  weight = 1,
  units = "mins",
  indifference = q(0.5),
  similarity = q(0.2),
  veto = NULL,
  name = "duration"
)
}
\arguments{
\item{weight}{A single positive weight (normalised later, when the
similarity matrix is built).}

\item{indifference, similarity, veto}{Thresholds, each a number or a \code{\link[=q]{q()}}
quantile specification. The defaults differ by direction and are
documented in the argument defaults.}

\item{name}{Criterion label.}

\item{method}{Edit-distance method passed to
\code{\link[stringdist:seq_distmatrix]{stringdist::seq_distmatrix()}}.}

\item{units}{Time unit for durations, passed to \code{\link[base:difftime]{base::difftime()}}.}
}
\value{
A \code{\link[=criterion]{criterion()}} object.
}
\description{
Convenience constructors that build a \code{\link[=criterion]{criterion()}} from a trace measure,
with sensible, overridable quantile-threshold defaults. The similarity
measures (\code{crit_activity_profile()}, \code{crit_transitions()}) point in the
\code{"similarity"} direction; the distance measures point in the
\code{"dissimilarity"} direction.
}
\details{
\itemize{
\item \code{crit_activity_profile()} -- cosine similarity of activity count vectors.
\item \code{crit_transitions()} -- cosine similarity of distance-weighted transition
profiles (weight \code{1 / gap}), as in the foundations paper.
\item \code{crit_edit_distance()} -- OSA edit distance on the integer-encoded traces.
\item \code{crit_trace_length()} -- \verb{|n_a - n_b|}, the difference in event counts.
\item \code{crit_distinct_activities()} -- difference in the number of distinct
activities.
\item \code{crit_duration()} -- difference in case duration.
}
}
\examples{
crit_activity_profile(weight = 0.2)
crit_edit_distance(weight = 0.2, similarity = 2, indifference = 3, veto = 6)
}
\seealso{
\code{\link[=crit_nominal]{crit_nominal()}}, \code{\link[=crit_ordinal]{crit_ordinal()}}, \code{\link[=crit_numeric]{crit_numeric()}},
\code{\link[=crit_custom]{crit_custom()}} for attribute-based criteria.
}
