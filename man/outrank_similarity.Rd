% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outranking.R
\name{outrank_similarity}
\alias{outrank_similarity}
\title{Outranking credibility matrix}
\usage{
outrank_similarity(traces, criteria, keep_partials = FALSE)
}
\arguments{
\item{traces}{A \code{traces} object (see \code{\link[=as_traces]{as_traces()}}); defines the case set
and their order.}

\item{criteria}{A \code{\link[=criterion]{criterion()}} object, or a non-empty list of them.}

\item{keep_partials}{If \code{TRUE}, retain the per-criterion measure,
concordance and discordance matrices for inspection.}
}
\value{
An object of class \code{outrank_sim}: a list with the credibility
matrix \code{S}, the aggregate concordance \code{C} and discordance \code{D}, the case
ids, the resolved criteria with normalised weights, and (optionally) the
partial matrices.
}
\description{
\code{outrank_similarity()} aggregates a set of criteria into the ELECTRE-III
credibility matrix \code{S}, the pairwise similarity used for clustering.
}
\details{
Each criterion contributes a partial concordance \code{c_j} and discordance
\code{d_j} (see \code{\link[=criterion]{criterion()}}). With weights \code{w_j} normalised to sum to one, the
aggregation is
\deqn{C(a, b) = \sum_j w_j\, c_j(a, b),}
\deqn{D(a, b) = 1 - \prod_{j \in J(a,b)} \frac{1 - d_j}{1 - c_j},
      \quad J(a,b) = \{ j : d_j(a,b) > c_j(a,b) \},}
\deqn{S(a, b) = \min\bigl(C(a, b),\, 1 - D(a, b)\bigr).}
A criterion with \code{c_j = 1} is never in \code{J} (concordance is already maximal),
which also avoids the division by \code{1 - c_j}.

Quantile thresholds (\code{\link[=q]{q()}}) are resolved against the off-diagonal
distribution of each criterion's own measure matrix before the indices are
computed. Weights are normalised to sum to one, with a message when they did
not already.
}
\examples{
log <- data.frame(
  case = rep(c("a", "b", "c"), each = 2),
  act  = c("x", "y", "x", "y", "x", "x"),
  ts   = as.POSIXct("2020-01-01") + c(0, 1, 0, 1, 0, 1) * 60
)
tr <- as_traces(log, "case", "act", "ts")
# A criterion on a precomputed similarity matrix (crit_* helpers wrap this).
m <- matrix(c(1, 0.8, 0.2, 0.8, 1, 0.3, 0.2, 0.3, 1), 3, 3,
            dimnames = list(c("a", "b", "c"), c("a", "b", "c")))
crit <- criterion(m, direction = "similarity",
                  indifference = 0.4, similarity = 0.9, veto = 0.1)
outrank_similarity(tr, crit)
}
