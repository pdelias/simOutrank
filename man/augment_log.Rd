% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{augment_log}
\alias{augment_log}
\title{Join cluster memberships onto an event log}
\usage{
augment_log(clust, log)
}
\arguments{
\item{clust}{An \code{outrank_clust} object from \code{\link[=cluster_traces]{cluster_traces()}}.}

\item{log}{The event-log \code{data.frame} the clustering came from.}
}
\value{
\code{log} with an added integer \code{.cluster} column (\code{NA} for cases absent
from the clustering, e.g. trimmed outliers).
}
\description{
\code{augment_log()} adds a \code{.cluster} column to an event log, mapping every
event to the cluster of its case.
}
\details{
The case-id column is detected as the column of \code{log} whose values
cover all clustered case ids; a message reports which column was used.
}
\examples{
log <- data.frame(case = rep(c("a", "b", "c", "d"), each = 2),
                  act = rep(c("x", "y"), 4),
                  ts = as.POSIXct("2020-01-01") + (0:7) * 60)
m <- matrix(0.1, 4, 4); m[1:2, 1:2] <- 0.9; m[3:4, 3:4] <- 0.9
diag(m) <- 1; dimnames(m) <- list(c("a", "b", "c", "d"), c("a", "b", "c", "d"))
crit <- criterion(m, "similarity", indifference = 0.3, similarity = 0.95)
tr <- as_traces(log, "case", "act", "ts")
clust <- cluster_traces(outrank_similarity(tr, crit), k = 2, seed = 1)
augment_log(clust, log)
}
