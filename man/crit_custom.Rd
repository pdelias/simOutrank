% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crit_attribute.R
\name{crit_custom}
\alias{crit_custom}
\title{Custom criterion}
\usage{
crit_custom(
  x,
  direction,
  weight = 1,
  indifference,
  similarity,
  veto = NULL,
  name = "custom"
)
}
\arguments{
\item{x}{A \verb{function(traces)} returning a symmetric numeric matrix, or a
precomputed numeric matrix with case-id dimnames.}

\item{direction}{\code{"similarity"} or \code{"dissimilarity"}.}

\item{weight}{A single positive weight.}

\item{indifference, similarity}{Required thresholds (number or \code{\link[=q]{q()}}).}

\item{veto}{Optional veto threshold.}

\item{name}{Criterion label.}
}
\value{
A \code{\link[=criterion]{criterion()}} object.
}
\description{
The escape hatch for criteria that the templates do not cover, including
composite measures (e.g. a linear combination of others). Wraps
\code{\link[=criterion]{criterion()}} directly.
}
\examples{
# A criterion on a precomputed similarity matrix.
m <- matrix(c(1, 0.7, 0.7, 1), 2, dimnames = list(c("a", "b"), c("a", "b")))
crit_custom(m, direction = "similarity", weight = 1,
            indifference = 0.4, similarity = 0.9)
}
