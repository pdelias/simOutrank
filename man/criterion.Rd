% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterion.R
\name{criterion}
\alias{criterion}
\title{Define an outranking criterion}
\usage{
criterion(
  measure,
  direction = c("similarity", "dissimilarity"),
  weight = 1,
  indifference,
  similarity,
  veto = NULL,
  name = NULL
)
}
\arguments{
\item{measure}{Either a \verb{function(traces)} returning a symmetric numeric
matrix, or a precomputed numeric matrix with dimnames matching the case
ids.}

\item{direction}{One of \code{"similarity"} (larger values mean more alike) or
\code{"dissimilarity"} (smaller values mean more alike).}

\item{weight}{A single positive number. Weights are normalised to sum to
one when the credibility matrix is built.}

\item{indifference, similarity}{Required thresholds; each a single number or
a \code{\link[=q]{q()}} quantile specification.}

\item{veto}{Optional veto threshold; a number, a \code{\link[=q]{q()}} specification, or
\code{NULL} for no discordance.}

\item{name}{Optional label used in printing and diagnostics.}
}
\value{
An object of class \code{criterion}.
}
\description{
\code{criterion()} is the core constructor behind the \verb{crit_*} helpers; users
rarely call it directly. It bundles a pairwise measure with its direction,
weight and ELECTRE-III-style thresholds.
}
\details{
For a criterion with indifference threshold \eqn{q}, similarity threshold
\eqn{s} and (optional) veto threshold \eqn{t}, and a pairwise value
\eqn{v = g(a, b)}, the partial concordance and discordance of a
\code{"similarity"}-direction criterion are
\deqn{c = 1 \ \mathrm{if}\ v \ge s, \quad
      c = \frac{v - q}{s - q} \ \mathrm{if}\ q \le v < s, \quad
      c = 0 \ \mathrm{if}\ v < q,}
\deqn{d = 1 \ \mathrm{if}\ v \le t, \quad
      d = \frac{q - v}{q - t} \ \mathrm{if}\ t < v \le q, \quad
      d = 0 \ \mathrm{if}\ v > q.}
For a \code{"dissimilarity"}-direction criterion the inequalities reverse. This
imposes an ordering on the thresholds, which the constructor validates:
\eqn{t < q < s} for the similarity direction and \eqn{s < q < t} for the
dissimilarity direction. A \code{veto = NULL} criterion contributes no
discordance.

Thresholds may be given as plain numbers or as a quantile specification
\code{\link[=q]{q()}}. Numeric thresholds are validated immediately; quantile thresholds
are resolved and validated once the measure matrix is known.
}
\examples{
# A similarity criterion on a precomputed matrix.
m <- matrix(c(1, 0.4, 0.4, 1), 2, dimnames = list(c("a", "b"), c("a", "b")))
criterion(m, direction = "similarity", weight = 2,
          indifference = 0.5, similarity = 0.8, veto = 0.2)
}
