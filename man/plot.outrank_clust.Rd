% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.outrank_clust}
\alias{plot.outrank_clust}
\title{Plot a trace clustering}
\usage{
\method{plot}{outrank_clust}(
  x,
  type = c("dendrogram", "eigenvalues", "profile"),
  attribute = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{outrank_clust} object from \code{\link[=cluster_traces]{cluster_traces()}}.}

\item{type}{One of \code{"dendrogram"} (the hierarchical tree; built on
\code{as.dist(1 - S)} when the clustering was spectral), \code{"eigenvalues"} (the
smallest normalized-Laplacian eigenvalues), or \code{"profile"} (the
distribution of a case \code{attribute} across clusters).}

\item{attribute}{For \code{type = "profile"}, the name of a case attribute
carried by the underlying \code{sim}.}

\item{...}{Passed to the underlying plotting call.}
}
\value{
\code{x}, invisibly.
}
\description{
Diagnostic plots for an \code{outrank_clust} object.
}
\examples{
m <- matrix(0.1, 6, 6); m[1:3, 1:3] <- 0.9; m[4:6, 4:6] <- 0.9
diag(m) <- 1; dimnames(m) <- list(1:6, 1:6)
crit <- criterion(m, "similarity", indifference = 0.3, similarity = 0.95)
tr <- as_traces(data.frame(case = as.character(1:6), act = "x",
                           ts = as.POSIXct("2020-01-01")),
                "case", "act", "ts")
clust <- cluster_traces(outrank_similarity(tr, crit), k = 2, seed = 1)
plot(clust, type = "eigenvalues")
}
