% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{eigengap}
\alias{eigengap}
\title{Eigenvalue gap diagnostic}
\usage{
eigengap(sim, k_max = 30)
}
\arguments{
\item{sim}{An \code{outrank_sim} object from \code{\link[=outrank_similarity]{outrank_similarity()}}.}

\item{k_max}{Number of smallest eigenvalues to show.}
}
\value{
Invisibly, a data frame with the eigenvalue \code{index}, the
\code{eigenvalue}, and the \code{gap} to the next one.
}
\description{
\code{eigengap()} plots the smallest eigenvalues of the symmetric normalized
Laplacian of \code{S}. A pronounced gap after the \code{k}-th eigenvalue suggests \code{k}
well-separated clusters.
}
\examples{
m <- matrix(0.1, 6, 6) + diag(0.9, 6)
m[1:3, 1:3] <- 0.9; m[4:6, 4:6] <- 0.9; diag(m) <- 1
dimnames(m) <- list(1:6, 1:6)
crit <- criterion(m, "similarity", indifference = 0.3, similarity = 0.95)
tr <- as_traces(
  data.frame(case = as.character(1:6), act = "x",
             ts = as.POSIXct("2020-01-01")),
  "case", "act", "ts"
)
eigengap(outrank_similarity(tr, crit), k_max = 5)
}
