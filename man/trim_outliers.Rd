% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{trim_outliers}
\alias{trim_outliers}
\title{Trim outlier cases}
\usage{
trim_outliers(sim, prop = 0.05, method = c("greedy", "lp"), lp_max = 150L)
}
\arguments{
\item{sim}{An \code{outrank_sim} object from \code{\link[=outrank_similarity]{outrank_similarity()}}.}

\item{prop}{Fraction of cases to remove (in \verb{[0, 1)}).}

\item{method}{\code{"greedy"} (default) or \code{"lp"}.}

\item{lp_max}{Size above which the \code{"lp"} method warns about its cost.}
}
\value{
A trimmed \code{outrank_sim}, with a \code{trimmed} attribute giving the
removed case ids.
}
\description{
\code{trim_outliers()} removes a fraction of the least-connected cases from an
\code{\link[=outrank_similarity]{outrank_similarity()}} result, before clustering.
}
\details{
Let \code{k = floor(prop * n)}. The \code{"greedy"} method removes the \code{k} cases with
the smallest total similarity to the others (row sums of \code{S} with the
diagonal excluded). The \code{"lp"} method solves the integer linear program of
Delias et al. (2021),
\deqn{\min_{o, r} \sum_{i,j} s_{ij} r_{ij} \quad \text{s.t.} \quad
  \sum_i o_i = k,\ o_i \le r_{ij},\ o_i \le r_{ji},\ o, r \in \{0, 1\},}
which selects the \code{k} cases whose incident edges carry the least similarity.
A small constant is added to \code{S} so that zero entries do not leave \code{r}
unconstrained. The LP has \code{n + n^2} binary variables and is intended for
small \code{n}; it warns above \code{lp_max}.
}
\examples{
m <- matrix(0.8, 5, 5); m[5, ] <- 0.05; m[, 5] <- 0.05; diag(m) <- 1
dimnames(m) <- list(1:5, 1:5)
crit <- criterion(m, "similarity", indifference = 0.3, similarity = 0.9)
tr <- as_traces(data.frame(case = as.character(1:5), act = "x",
                           ts = as.POSIXct("2020-01-01")),
                "case", "act", "ts")
sim <- outrank_similarity(tr, crit)
trim_outliers(sim, prop = 0.2)
}
\references{
Delias, P. et al. (2021). Improving the non-compensatory trace
clustering. \emph{International Transactions in Operational Research}.
}
