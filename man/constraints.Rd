% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{constraints}
\alias{constraints}
\alias{must_link}
\alias{cannot_link}
\title{Must-link and cannot-link constraints}
\usage{
must_link(sim, pairs_or_attribute, reward = 2)

cannot_link(sim, pairs_or_attribute)
}
\arguments{
\item{sim}{An \code{outrank_sim} object from \code{\link[=outrank_similarity]{outrank_similarity()}}.}

\item{pairs_or_attribute}{A two-column matrix of case-id pairs, or a
length-one attribute name (requires that \code{sim} carries case attributes).}

\item{reward}{Positive amount added to \code{S} for each must-linked pair.}
}
\value{
The \code{outrank_sim} with an updated \code{S}.
}
\description{
Inject domain knowledge into the credibility matrix by rewarding pairs that
should share a cluster (\code{must_link()}) or severing pairs that should not
(\code{cannot_link()}), following Delias et al. (2021).
}
\details{
With a symmetric 0/1 constraint mask \code{M} (zero diagonal),
\deqn{\text{must\_link:}\quad S \leftarrow \min(S + \text{reward}\cdot M,\ 1),}
\deqn{\text{cannot\_link:}\quad S \leftarrow S \odot (1 - M).}
The constraint set is given either as a two-column matrix of case-id pairs,
or as the name of a case attribute: \code{must_link()} then links cases with an
equal attribute value, and \code{cannot_link()} severs cases whose values differ.
}
\examples{
m <- matrix(0.2, 4, 4); diag(m) <- 1; dimnames(m) <- list(1:4, 1:4)
crit <- criterion(m, "similarity", indifference = 0.1, similarity = 0.9)
tr <- as_traces(data.frame(case = as.character(1:4), act = "x",
                           ts = as.POSIXct("2020-01-01")),
                "case", "act", "ts")
sim <- outrank_similarity(tr, crit)
sim <- must_link(sim, cbind("1", "2"))
cannot_link(sim, cbind("3", "4"))$S
}
